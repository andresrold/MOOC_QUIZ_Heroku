    <p>Pregunta de <%= quiz.tema %></p>
    <form method="get" action="/quizes/<%= quiz.id %>/answer">
        <label>Pregunta: <%= quiz.pregunta %></label>
        <input type="text" name="respuesta" placeholder="Responda aquí" />
        <input type="submit" value="Enviar" />
    </form>
<br />
<h3>Comentarios (<%= quiz.Comments.length %>)</h3>
<% for(var i=0; i<quiz.Comments.length; i++) { %>
    <p>-> <%= quiz.Comments[i].texto %></p>
<% } %>
<hr />
<a href="/quizes/<%= quiz.id %>/comments/new"><button>Nuevo comentario</button></a>
